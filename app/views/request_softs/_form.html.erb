<%= form_with(model: request_soft) do |form| %>
  <% if request_soft.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(request_soft.errors.count, "error") %> prohibited this request_soft from being saved:</h2>

      <ul>
        <% request_soft.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
