<%= form_with(model: request_soft_aud) do |form| %>
  <% if request_soft_aud.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(request_soft_aud.errors.count, "error") %> prohibited this request_soft_aud from being saved:</h2>

      <ul>
        <% request_soft_aud.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :request_soft_id, style: "display: block" %>
    <%= form.text_field :request_soft_id %>
  </div>

  <div>
    <%= form.label :comp_class_id, style: "display: block" %>
    <%= form.text_field :comp_class_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
